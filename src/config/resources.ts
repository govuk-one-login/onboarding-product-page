import path from "path";

export const resources = path.join(__dirname, "../../resources");
export const views = path.join(__dirname, "../../src/views");

export const allowedDomains = path.join(resources, "allowed-email-domains.txt");
